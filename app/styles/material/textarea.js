import { makeStyles } from '@material-ui/core/styles';

export const materialTextAreaStyles = makeStyles({
  formControl: {
    padding: '16px 24px',
    fontSize: '16px',
    border: '1px solid #828282',
    borderRadius: '4px',
    marginTop: '8px',
    marginBottom: 0,
    minWidth: '100%',
    maxWidth: '100%',
  },
});
